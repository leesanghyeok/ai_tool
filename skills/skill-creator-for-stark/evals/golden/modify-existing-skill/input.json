{
  "mode": "modify",
  "request": "기존 skill package의 workflow와 검증 절차를 개선하고 실제 검증 output을 보고해줘.",
  "expected_checks": [
    "existing eval contract is preserved or updated",
    "changed paths are read back",
    "validator and git diff --check are executed",
    "quality rubric is required when workflow, approval, verification, trigger, scope, template, or validator changes materially"
  ]
}
